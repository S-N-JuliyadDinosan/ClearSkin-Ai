CREATE TABLE staff (
                       staff_id BIGINT PRIMARY KEY,
                       email VARCHAR(255),
                       password VARCHAR(255),
                       role VARCHAR(50)
);
