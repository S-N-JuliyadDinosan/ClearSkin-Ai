CREATE TABLE doctor (
                        doctor_id BIGINT PRIMARY KEY AUTO_INCREMENT,
                        name VARCHAR(255),
                        qualifications TEXT,
                        speciality VARCHAR(255)
);
